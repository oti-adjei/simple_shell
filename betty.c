#include <stdio.h>

/**
 * main - Entry point of the Simple Shell Project
 *
 * Return: Always 0 (Success)
 */
int betty_main(void)
{
	int i = 0;

	while (i < 5)
	{
		printf("Simple Shell Project\n");
		i++;
	}

	return (0);
}

